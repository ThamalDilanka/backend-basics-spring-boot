spring.application.name=plant-swap-api
# Database Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/plant_swap_db
spring.datasource.username=ajay
spring.datasource.password=20212022

# JPA / Hibernate Settings
# This tells Spring to automatically create tables based on your Java classes
spring.jpa.hibernate.ddl-auto=update

# This shows the SQL queries in the console (useful for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Database Platform
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect